# pipenv

manage pip and virtualenv

```
#Install pipenv
pip3 install pipenv
# Pipenv shell
pipenv shell
# Exit pipenv shell
exit
# run command
pipenv run {file.py}
```

# pip
```
# find the config file
python3 -m pip config debug
```
